# Laravel for Vite’s /api proxy. Port 8000 is often occupied by several processes on Windows;
# use 8001 for this app so login and API routes always hit the right server.
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8001
